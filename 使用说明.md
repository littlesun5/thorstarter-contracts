# BTC价差监控系统 - 使用说明

## 项目概述

这是一个用于监控Backpack和Lighter两个交易所BTC价格差异的GUI应用程序，可以每小时自动记录价差数据。

## 文件说明

- `btc_monitor.py` - 主程序（GUI界面，需要浏览器支持）
- `test_gui.py` - 测试程序（使用模拟数据，不需要浏览器）
- `price_fetcher.py` - 价格获取模块
- `data_storage.py` - 数据存储模块
- `requirements.txt` - Python依赖包
- `run_monitor.sh` - 启动脚本

## 安装步骤

### 1. 安装系统依赖

```bash
# 安装Python3和tkinter
sudo apt-get update
sudo apt-get install -y python3 python3-tk

# 安装Chrome/Chromium浏览器（用于网页抓取）
sudo apt-get install -y chromium-browser chromium-chromedriver
```

### 2. 安装Python依赖

```bash
pip3 install -r requirements.txt
```

## 使用方法

### 方法一：运行主程序（需要浏览器）

```bash
python3 btc_monitor.py
```

**注意**：主程序需要Chrome/Chromium浏览器来抓取网页价格。如果浏览器无法启动，程序会尝试使用API，但可能无法获取价格。

### 方法二：运行测试程序（使用模拟数据）

```bash
python3 test_gui.py
```

测试程序使用模拟数据，可以完整展示GUI界面和所有功能，适合在没有浏览器或网络环境受限的情况下测试。

### 方法三：使用启动脚本

```bash
chmod +x run_monitor.sh
./run_monitor.sh
```

## 功能说明

### GUI界面功能

1. **实时价格显示**
   - Backpack交易所价格（蓝色框）
   - Lighter交易所价格（红色框）
   - 价差显示（绿色框，包含美元和百分比）

2. **控制按钮**
   - **开始监控**：启动每小时自动记录功能
   - **停止监控**：停止自动记录
   - **立即刷新**（主程序）或**模拟更新**（测试程序）：手动获取/更新价格

3. **历史数据表格**
   - 显示所有记录的历史价差数据
   - 包含时间戳、两个交易所价格、价差和价差百分比

### 数据存储

- 主程序数据保存在：`btc_price_data.csv`
- 测试程序数据保存在：`btc_price_data_test.csv`

数据格式：
```
时间,Backpack价格,Lighter价格,价差(USD),价差百分比(%)
2024-01-01 12:00:00,95000.00,94800.00,200.00,0.2109
```

## 故障排除

### 1. 无法启动GUI

**问题**：`ModuleNotFoundError: No module named 'tkinter'`

**解决**：
```bash
sudo apt-get install -y python3-tk
```

### 2. 无法获取价格

**问题**：价格显示"获取中..."或"无法获取价格"

**可能原因**：
- Chrome/Chromium未正确安装
- 网络连接问题
- 目标网站结构变化

**解决**：
- 检查浏览器安装：`which chromium-browser`
- 尝试运行测试程序验证GUI功能
- 检查网络连接
- 查看控制台日志输出

### 3. Chrome启动失败

**问题**：`Chrome failed to start` 或 `cannot find Chrome binary`

**解决**：
```bash
# 安装Chromium
sudo apt-get install -y chromium-browser

# 验证安装
chromium-browser --version
```

### 4. 在无显示服务器环境中运行

如果是在服务器环境中（没有图形界面），可以：
1. 使用SSH X11转发：`ssh -X user@server`
2. 或者修改代码使用虚拟显示：安装`xvfb`并使用`xvfb-run python3 btc_monitor.py`

## 技术架构

- **GUI框架**：tkinter（Python标准库）
- **网页抓取**：Selenium + ChromeDriver
- **数据处理**：pandas
- **定时任务**：schedule
- **数据存储**：CSV文件

## 注意事项

1. 首次运行时会自动下载ChromeDriver
2. 程序使用无头浏览器模式，不会显示浏览器窗口
3. 某些网站可能有反爬虫机制，如果频繁失败，可以尝试增加等待时间
4. 建议在有图形界面的环境中运行GUI程序
5. 如果只需要测试功能，使用`test_gui.py`更方便

## 开发说明

### 价格获取逻辑

程序按以下顺序尝试获取价格：
1. 首先尝试使用公共API（如果可用）
2. 如果API失败，使用Selenium抓取网页
3. 使用多种选择器和正则表达式提取价格

### 扩展功能

可以轻松扩展的功能：
- 添加更多交易所
- 添加价格预警功能
- 添加图表显示
- 导出Excel报告
- 添加邮件/通知功能

## 许可证

MIT License
